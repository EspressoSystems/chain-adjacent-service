name: E2E Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-Dwarnings"

jobs:
  e2e-v3_9_9:
    name: E2E Tests (Nitro v3.9.9)
    runs-on: ubuntu-24.04-8core
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull Docker images
        working-directory: e2e/nitro
        run: |
          set -a
          source .env
          source .env.v3_9_9
          set +a
          docker pull "$NITRO_IMAGE" &
          docker pull "$ESPRESSO_DEV_NODE_IMAGE" &
          docker pull ghcr.io/foundry-rs/foundry:latest &
          docker pull ghcr.io/foundry-rs/foundry:v1.5.1 &
          wait

      - uses: extractions/setup-just@v3

      - name: Run E2E tests
        run: just test-e2e-v3_9_9

  e2e-v3_10:
    name: E2E Tests (Nitro v3.10)
    runs-on: ubuntu-24.04-8core
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull Docker images
        working-directory: e2e/nitro
        run: |
          set -a
          source .env
          set +a
          docker pull "$NITRO_IMAGE" &
          docker pull "$ESPRESSO_DEV_NODE_IMAGE" &
          docker pull ghcr.io/foundry-rs/foundry:latest &
          docker pull ghcr.io/foundry-rs/foundry:v1.5.1 &
          wait

      - uses: extractions/setup-just@v3

      - name: Run E2E tests
        run: just test-e2e
